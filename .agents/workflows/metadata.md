---
description: Generate AI metadata for papers
---
// turbo-all

Run the following command to generate AI metadata for each paper in the database. Adjust `--db` according to the user's request.

Default example:
```bash
cd arxiv-3d
python process_ai_metadata.py --db papers_astrophysics.db
```
