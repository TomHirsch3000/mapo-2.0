---
description: Fetch missing abstracts from Semantic Scholar and ArXiv
---
// turbo-all

Run the following command to fetch missing abstracts. Adjust `--db` according to the user's request.

Default example:
```bash
cd arxiv-3d
python fetch_abstracts_s2_arxiv.py --db papers_astrophysics.db
```
