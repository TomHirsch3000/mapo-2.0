---
description: Rebuild citation edges
---
// turbo-all

Run the following command to rebuild citations within the local database. Adjust `--db` according to the user's request.

Default example:
```bash
cd arxiv-3d
python rebuild_citations_openalex.py --db papers_astrophysics.db
```
